import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.photoljay.app',
  appName: 'PhotoLJay',
  webDir: 'dist/frontend/browser'
};

export default config;
